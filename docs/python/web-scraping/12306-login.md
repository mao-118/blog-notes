# 模拟12306登录

## 此十 |

### 模拟 12306 登录

### 首先, 我们通过 selenium 打开 12306 的登录页面. 并进入到账号登录

模块

```python
web = Chrome()
```

```python
web.get("https://kyfw. 12306.cn/otn/resources/Login
```

```python
time.sleep(1)
```

web.find_element_by_xpathC'/html/body/div[2]/div[2

```python
time.sleep(i1)
```

找到验证码位置, 并截图, 对验证码进行解析.

```python
verify_img = web.find_element_by_xpathC'//*
[@id="J-LoginImg"]')
result =
```

chaojiying.PostPic(verify_img.screenshot_as_png,

9004)

在验证码区域模拟用户点击

```python
points_str_lst = result['pic_str'].splitcC"!")
for p in points_str_lst:
    p_temp = p.spLlit(",")
    p_x = int(p_temp[Q])
    p_y = int(p_temp[1])
```

```python
 ActionChains(web).move_to_eLement_with_offset(ver
ify_img, p_x, p_y).click().perform()
```

### 然后输入用户名和密码, 并点击登录按钮

```python
time.sleep(1)
web.find_element_by_xpath('//*[@id="J-
```

userName"]').send_keysC"homexue@126.com")

```python
password" |').send_keys('q6035945")
time.sleep(1)
web.find_element_by_xpath('//*[@id="J-
```

## 最后一步, 进入滑块环节, 这里其实看似容易, 操作起来就恶心了.

### 为这里才是本节真正要讲的. 12306 会在这里进行浏览器验证, 验证

### 你是否是通过自动化工具局动的浏览器. 而且, 这个问题非常恶心, 常

规的浏禹器检测处理方案都是没用的.

### 注意, 此时我们要了解一下这个插件的工作机制. 它是在整个页面加

### 载的时候就开始对浏览器参数进行读取. 所以我们常规的对 Chrome

设置是无效的. 此时, 需要添加以下一段代码来规避检测.

```python
# 杀测，88 版本以后可以用.
option = Options()
#
```

option.add_experimental_optionC'excLudeSwitches',

["enablLe-aqutomation'])

option.add_argument('--disable-bLink-

```python
features=AutomationControLlled')
web = ChromeCoptions=option)
# 杀测，88 版本之前可以用
```

![模拟12306登录 - 第3页](assets/12306-login/page-3.png)

```python
# web = Chrome()
#
#
```

web.execute_cdp_cmd("Page.addScriptToEvaLuateOnNe

wDocument", {

{

```python
# get: (©) => undefined
# 了)
# uu
#})
```

最后给出完整代码

```python
from selenium.webdriver import Chrome
from selenium.webdriver.chrome.options import
```

Options

```python
from selenium.webdriver.common.action_chains
import ActionChains
from chaojiying import Chaojiying_Client
import time
```

```python
chaojiying = Chaojiying_CLient(C xxxxxxx",
```

])

option.add_argument('--disable-blink-

```python
features=AutomationControLlled')
web = ChromeCoptions=option)
# 杀测，88 版本之前可以用.
# web = Chrome()
#
#
```

wDocument", {

{

```python
# get: (©) => undefined
```

```python
# 了)
```

```python
# 了)
```

```python
web.get("https://kyfw. 12306.cn/otn/resources/Logi
```

n. html")

```python
time.sleep(1)
```

web.find_eLement_by_xpathC'/html/body/div[2]/divL

```python
time.sleep(1)
```

```python
verify_img = web.find_element_by_xpath('//*
```

```python
result =
```

9004)

```python
points_str_Lst = resultL'pic_str'].splitc"!")
for p in points_str_lst:
    p_temp = p.spLlit(",")
    p_x = int(p_temp[Q])
    p_y = int(p_temp[1])
```

```python
ActionChains(web).move_to_eLement_with_offset(ve
```

rify_img, p_x, p_y).clickQ).performQ)

```python
time.sleep(1)
web.find_element_by_xpath('//*[@id="J-
```

userName"]').send_keysC"xxxxxxx@126.com")

```python
web.find_element_by_xpath('//*[@id="J-
password"]|').send_keys(C' xxxxxxx")
time.sleep(1)
web.find_eLement_by_xpath('//*[@id="J-
```

```python
time.sleep(3)
btn = web.find_element_by_xpath('//*
[@id="nc_1_niz"]")
ActionChains(web).drag_and_drop_by_offset(btn,
```
